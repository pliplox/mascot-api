filter:
  branches:
    only:
      - master
      - develop
jobs:
  test:
    executor: node/default
    steps:
      - checkout
      - node/install-packages:
          cache-path: ~/mascot-web/node_modules
          override-ci-command: npm install
      - run: npm run test
orbs:
  node: 'circleci/node:13.12.0'
version: 2.1
workflows:
  tests:
    jobs:
      - test