// const userController = require('./usersController');

// This is a temporary test to build circle ci integration
// TO-DO: Test the controller with jest
test('returns true', () => {
  expect(true).toBe(true);
});
